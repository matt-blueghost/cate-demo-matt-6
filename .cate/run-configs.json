{
  "configurations": [
    {
      "id": "dev-server",
      "name": "Run Dev Server",
      "commands": [
        {
          "id": "dev",
          "label": "Dev Server",
          "command": "npm install && npm run dev",
          "cwd": "."
        }
      ]
    }
  ]
}
